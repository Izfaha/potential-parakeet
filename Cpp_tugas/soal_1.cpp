#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    /* for declaring variable in the question */
    double a = 2; // Deklarasi a 
    double exponent = 3; // Deklaration of exponent
    double b = 7; // Deklasrai b = 7

    // Formula for y "using pow" with math.h library 
    double y = pow(2, 3) + b; 
    cout << "Jadi y adalah " << y << "\n";
    return 0;
}